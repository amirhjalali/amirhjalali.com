name: Test Dispatch

on:
  workflow_dispatch:
    inputs:
      test:
        description: 'Test input'
        required: false

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Echo test
        run: echo "Test workflow dispatch works"

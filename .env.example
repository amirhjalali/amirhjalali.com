# Site URL Configuration (Required)
# Your production site URL - used for sitemap, SEO, and canonical URLs
NEXT_PUBLIC_SITE_URL=https://gaboojabrothers.cloud

# OpenAI API Configuration (Required for AI content generation)
# Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Google Analytics (Optional)
# Get your measurement ID from Google Analytics
# NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Database Configuration (Required for notes and articles)
DATABASE_URL=your_database_url_here

# Redis Configuration (Required for background job processing)
# Local development: redis://localhost:6379
# Production: Use Coolify Redis service or external provider (Upstash, Redis Cloud)
REDIS_URL=redis://localhost:6379

# Gemini API Configuration (For article generation with references)
# Get your API key from https://aistudio.google.com/
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: Additional AI Service Keys
# ANTHROPIC_API_KEY=your_anthropic_key_here
# REPLICATE_API_TOKEN=your_replicate_token_here

# Admin Authentication (client-side for static sites)
# Note: These values are baked into the build. Prefer using a SHA-256 hash.
NEXT_PUBLIC_ADMIN_USERNAME=admin
# Generate SHA-256 hash for your admin password and place it below.
NEXT_PUBLIC_ADMIN_PASSWORD_HASH=
# (Optional) Plain text fallback if you haven't set a hash yet. Remove once hash is configured.
NEXT_PUBLIC_ADMIN_PASSWORD=

# Base Path Configuration
# IMPORTANT: Leave empty for local development!
# Set to '/amirhjalali.com' (or your GitHub Pages repo name) only in production
# For local dev:  NEXT_PUBLIC_BASE_PATH=
# For GitHub Pages: NEXT_PUBLIC_BASE_PATH=/amirhjalali.com
NEXT_PUBLIC_BASE_PATH=
